include:
  - project: 'papers/papers-internal/internal'
    file: '/.base-gitlab-ci.yml'

stages:
  - build
  - publish
  - deploy

build:
  stage: build
  extends: .build

publish-dev:
  stage: publish
  extends: .publish-dev

publish-prod:
  stage: publish
  extends: .publish-prod
